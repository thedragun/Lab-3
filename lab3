#include <iostream>
#include <cmath>
#include <cstdlib>
#include <time.h>
#include <stdio.h>

using namespace std;

int main() {

int input;
int range;

srand(time(NULL));

std::cout << "please enter the amount of numbers you want to test:" << '\n';
std::cin >> numAmount;

std::cout << "please enter the range you want to generate in:" << '\n';
std::cin >> range;


for (size_t limiter = 0; limiter < numAmount; limiter++) {
  int randomNumberSet = rand() % range + 1;
}

  return 0;
}
